# 2026-09-08

## Docker Desktop 路线（当日完成）
- 选择了 A 方案（Docker Desktop + 保留两台虚拟机作部署目标）。
- WSL2 安装踩坑：`wsl --install` 报 403（微软商店被墙）→ 手动 dism 启用两个功能 + GitHub 下载 wsl.2.9.9.0.x64.msi（344MB）解决。讲清了 WSL2 概念（与 VMware 对比：轻量/秒开/无快照，dev 工具 vs 完整服务器）。
- Docker Desktop 安装：用户坚持装 D 盘。首次图形化安装实际未完成（AppData\Local\Docker 只有日志）；最终用管理员 PowerShell 静默安装成功：`install --quiet --accept-license --installation-dir=D:\softwares\Docker --wsl-default-data-root=D:\softwares\Docker\wsl-data`，程序 3.3GB 全在 D 盘，C 盘零占用。用户跳过 Docker 登录（已解释：本地 build/run 不需要登录，push 才要）。
- backend/Dockerfile 已创建并验证：多阶段构建（maven:3.9-eclipse-temurin-21 编译 + eclipse-temurin:21-jre-alpine 运行）、pom 先行分层缓存、阿里云 Maven 源（backend/docker/maven-settings.xml）、非 root 用户、TZ、JAVA_OPTS 可配。
- 关键坑：Docker Desktop 不读 ~/.docker/daemon.json（它由 GUI 管理引擎配置）→ registry-mirrors 无效 → 改为 Dockerfile FROM 直接写 docker.m.daocloud.io/library/... 加速源，成功。长期方案：Settings → Docker Engine GUI 里补 registry-mirrors（用户待做）。
- docker 命令不在 PATH：老终端窗口没刷新 PATH，重开窗口解决；临时可用完整路径 D:\softwares\Docker\resources\bin\docker.exe。
- research-agent:1.0 镜像构建成功（首次 build 全程通过），容器内 H2 dev 模式启动正常。用户首跑漏 -p、又在镜像页找"停止"（混淆镜像/容器）均已纠正；带 -p 8080:8080 重跑后 curl localhost:8080/api/tasks 返回 200 []，本机容器化全链路打通。已建议体验分层缓存（改代码重 build 出 1.1）。
- 下一步：docker save 导出镜像 → scp 传输 → node1 docker load + run（8081 端口）→ Windows curl http://192.168.157.128:8081/api/tasks 返回 200，"本机构建 → 服务器部署"链路全部打通（2026-09-08 完成）。node1 上的 research-agent 容器已配 --restart=always，tar 包已清理。
- 容器化学习阶段整体收官。后续路线：CI/CD 概念（GitLab CI/Jenkins 自动化这条手工链路）→ K8s（node1 装 K3s 或 Docker Desktop K8s，部署 research-agent 练 apply/scale/rolling update）。

## K3s/K8s 阶段（当日，2026-09-08 下半程）
- K3s 实际装在 node2（用户当时在 node2 上执行了安装，将错就错继续）。⚠️ NAT IP 动态变化：用户明确 node2 当前为 192.168.157.128（勿再用 .129/.157.157 等旧值；以 hostname 判断机器为准）。
- node2 磁盘偏小（LVM 根分区原 9.8G，Docker 全家桶 + K3s 塞满触发 ephemeral-storage Evicted + 节点污点导致新 Pod Pending）→ VMware 已扩磁盘+内存，系统内 growpart/pvresize/lvextend/resize2fs 扩容三连完成（预计 ~38G）。
- K3s 踩坑链：① pause 沙箱镜像被墙 → /etc/rancher/k3s/registries.yaml 配 docker.m.daocloud.io 加速 ② k3s.yaml 每次重启权限重置回 600 → 永久方案：cp 到 ~/.kube/config + chown + 删 .bashrc 的 KUBECONFIG export ③ k3s ctr 导入必须带 -n k8s.io 命名空间 ④ research-agent tar 本地 docker save 生成损坏（short read 同 blob 报错、两端 sha 一致却导不进）→ 已重建 tar。
- research-agent 已在 node2 K8s 跑通：~/k8s-lab/research-agent.yaml（Deployment replicas:2 + NodePort Service 30081），curl 200 [] 验证通过，kubectl get pods 仅剩 2 Running。
- 下一步：scale 2→3、自愈（删 Pod 观察自动重建）、滚动更新（需 Windows build research-agent:1.1 + 导入 node2 k8s.io）→ 逐行讲解 YAML 收尾。
- 当日结尾状态：磁盘 LVM 扩容已完成（根分区 9.8G→38G，可用 28G），内存已扩 6G；驱逐风暴平息。教训补充：kubelet 磁盘压力污点不会自动快速消失（曾手动 kubectl taint nodes ...- 删除，报 not found = 已自愈）；containerd 会 GC 无 Pod 引用的镜像（k8s.io 里 research-agent 曾被清空需重导）。scale/自愈实验通关，滚动更新演示中断：1.1 镜像当时未真正导入导致 ErrImagePull → 已 rollout undo 回 1.0，2 Pod Running 服务恢复（curl 30081 通）。
- 明日可选项：补完整滚动更新（Windows build 1.1→校验导入→set image→rollout status）→ 逐行精讲 YAML → 快照存档。docker build 时后端代码已加 "// build 1.1 demo" 注释标记（若已执行）。
- K8s 阶段完整收官（2026-09-08）：1.1 镜像成功构建（ResearchAgentApplication 加了 version 1.1 标记日志）、导入 k8s.io、滚动更新全程零停机跑通、rollout history 4 版本演进、rollout undo 回滚/回退演练完成。最终环境停在 research-agent:1.1（2 副本 Running，curl 30081 正常）。知识点：声明式(apply) vs 命令式(set image/rollout)混用的 last-applied-configuration 警告、rollout history 只保留近版本、kubectl logs deployment 随机挑 Pod（用 -l 精确）、kubectl rollout undo --to-revision。
- 建议收尾动作：VMware node2 拍快照"K3s-research-agent-1.1"存档；node1 也需同样 LVM 磁盘扩容（10G 小盘，迟早爆）。后续：YAML 逐行精讲 / Ingress / 双节点(k3s agent join node1) / CI/CD。
- 2026-09-08 已生成脱会话可读实战复盘文档：`docs/服务架构/虚拟机DockerK8s实战复盘.md`（含 A~E 各阶段命令、P1-P15 踩坑大全、LVM 扩容解法、环境状态清单、待办列表、概念速查表）。后续学习中如环境有变（IP/版本/新增坑）记得回更新该文档。
- 复盘文档已二次扩写（990 行）：新增 B.5 docker run 逐参数拆解、B.6 Volume 三方案详解（bind/named/anon + 自动创建 root 属主坑 + 空目录遮盖默认配置坑 + :ro 只读挂载 + compose 对照）、附录"命令详解大全"十二~十五章（Linux 高频命令按 7 类逐 flag；ssh/scp/ssh-keygen 逐 flag；Docker 按镜像/容器/管理分组逐 flag + 上生产命令串；kubectl 按查询/部署/发布回滚/污点分组逐 flag + K3s 特有命令 + 通用 flag 记忆法）。用户偏好：命令必须逐参数讲解，不要只给命令不给 flag 含义。
- 复盘文档已加"📖 目录"（章节内链，正文 一~十一 + 附录 十二~十五，含 B/E 小节跳转）。用户还咨询过云服务器采购配置（本地 2 VM 学习 vs 真上云跑多节点 K8s+Spring Cloud 的最低/推荐配置，已给清单与官方链接）。
- 复盘文档三度扩写（1209 行）：新增「十六、YAML/Dockerfile 逐行精读」（16.1 Dockerfile 逐行表、16.2 docker-compose 逐行+常用命令+与 K8s 对应、16.3 Deployment+Service 逐行+部署命令拆解+状态对照）。目录已同步。用户要求：部署类 YAML 必须有逐行解析版本，不能只有原文。
