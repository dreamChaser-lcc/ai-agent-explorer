# 2026-09-19

## 探针（Probe）主题：概念 + 路线 A 实战（已全部完成 ✅）

- 用户回归（距 09-17 两天）。先问"探针是什么意思、干嘛的"→ 完成概念讲解：探针=kubelet 定期健康检查；三类型（readiness=失败摘流量不重启 / liveness=失败重启 / startup=慢启动保护）；三方式（httpGet/tcpSocket/exec）；参数（initialDelaySeconds/periodSeconds/failureThreshold/timeoutSeconds）；结合 09-17 "Pod Running ≠ 就绪"的坑串讲。
- 现状确认：micro-lab 无 actuator 依赖（搜索 pom 0 结果）；父 POM repackage 修复完好。

## 路线 A（tcpSocket 探针）实战完成

1. 基线实验（无探针）：`rollout restart deployment/gateway` + curl 循环 → 观测到 `000`（真实中断）；
2. 加探针：重写 `~/k8s-lab/micro-lab.yaml` v2（三个 Deployment 均加：readiness tcpSocket [initialDelay 5 / period 5 / failureThreshold 6]；liveness tcpSocket [40 / 10 / 5]），旧版备份为 `micro-lab.yaml.bak-noprobe`；
3. 对比实验：**探针后全程 200，零中断滚动更新验证通过** ✅
4. 观察特征：新 Pod 经历 `0/1 Running → 1/1 Running`（未就绪窗口可见）。
- 参数黄金法则：**liveness 容忍时间必须 > 应用最长启动时间**（否则误杀 → CrashLoopBackOff）；readiness 失败不重启只摘流量，参数可从宽。
- 关键认知：探针定义在 Pod spec（Deployment YAML）里、**与镜像无关**——本次改动零成本，无需重建镜像。

## 文档同步

- 《微服务实战复盘.md》：新增 9.8 小节「探针与零中断发布」（含机制链、参数法则、对比实验金标准）；学习路线表加 ⑨；踩坑表加 #18（liveness 参数必须 > 启动时间）；待办清单更新（路线 A 勾选 + 路线 B 新增）。

## 下一步候选

- **路线 B**：加 `spring-boot-starter-actuator` → httpGet `/actuator/health`（含 /liveness /readiness 专用端点），能感知 Nacos 等依赖——需重建镜像、复习完整交付链路（build→save→scp→ctr import→rollout）；
- preStop hook + 优雅停机（保住老 Pod 退场瞬间的连接）；
- 或换成 C（微服务套 Ingress 域名）/ D（迁移真实项目 ai-agent-explorer）。

## 路线 B 执行中（09-19）

- 用户选路线 B（曾问 B vs preStop 企业常用度 → 结论：B 更标配——Spring Boot 官方支持 + actuator 生态 + 面试必问；preStop 是成熟团队发布规范细节，管"零中断的出口侧"，零镜像成本可顺手补）。
- 已完成：**三个模块 pom.xml 已加 `spring-boot-starter-actuator` 依赖**（user-service / order-service / gateway，本地代码库直接修改，我操作）。
- 待用户执行：mvn package → docker build×3 → save×3 → scp 两台 → ctr import×2 → YAML v3（探针改 httpGet：/actuator/health/readiness + /actuator/health/liveness；env 加 `MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED=true` 打开 probes 子端点）→ apply → 验证（`kubectl exec ... wget` 看 health 输出 + 零中断复跑）。

## 路线 B ✅ 完成（09-19 下午）

- 全流程走通：3 个 pom 加 actuator（本地代码已改）→ 用户 `mvn package`（10:01 三份 fat jar，本机验证均含 actuator entries=2）→ docker build/save/scp → 两台 ctr import → YAML v3 apply → **gateway 新 Pod 1/1、老 Pod 退场，滚动更新收口** ✓（gateway 就绪即证明 httpGet 探针走通了）。
- **大坑（同名旧 tar 冒充新镜像）**：gateway 新 Pod 永远 0/1（探针 404 + liveness 反复重启）；根因 = **当天 `docker save gateway` 那条命令没跑成**，`gateway-1.0.0.tar` 还是 09-17 的旧文件（同名覆盖肉眼不可辨）；两台 digest 一致但为旧值 `38964612...`。修复：单点重做 build/save（核对 tar 时间戳）→ scp → import（核对 digest 变化）→ 删 Pod 重建 → 1/1 ✓
- 三道防线（已向用户讲透）：① save 后 `dir` 看时间戳；② import 后 `ctr images ls` 看 digest 指纹；③ 进阶：用不可变 tag 替代同名覆盖。
- 文档同步：《微服务实战复盘.md》新增 9.9 小节（actuator/httpGet 全录 + 大坑复盘）+ 踩坑表加 #19 + 学习路线 ⑨ 更新 + 待办清单路线 B 勾选。
- **剩余尾巴**：① node2 IP 未收敛（K8s 显示 .130、目标 .129；疑双地址并存，待 `ip addr` 确认后收尾）② preStop/优雅停机未做（零镜像成本，可顺手补）③ httpGet 版零中断复跑可补测 ④ 拍快照（双节点静态 IP + 探针完成）。

## 当日收尾：回顾 + 文档详细补充（09-19 晚）

- 用户跑通"深入实验一/二"（show-details 打开 components 实录、启动窗口 503 抓取），并贴出完整 health JSON：discoveryComposite（含服务名单 gateway/user/order）、nacosConfig、nacosDiscovery、livenessState、readinessState、diskSpace、refreshScope、ping、groups。
- 用户要求"回顾总结今日内容 + 详细补充文档（实战过程 + 疑问）"。
- **《微服务实战复盘.md》9.9 扩充**：新增"深入实验（show-details 开关 + components 逐项解读 + 抓启动窗口 503）" + "当日答疑记录 Q1~Q3（两种探针本质差别 / 怎么读内部状态 / deploy 前缀语法）"。
- **《虚拟机DockerK8s实战复盘.md》更新**：目录（E.10 / P1-P20 / 日期）；1.3 主机与 IP 改写为"静态化"说明；**新增 E.10 IP 静态化战役**（导火索/根因链/两阶段法/DHCP 池避让/K3S_URL 修复/netplan 权限警告/OVS 噪音/agent 配置文件位置/诊断口诀）；踩坑表 +P19（节点静默掉线）+P20（同名旧 tar）；状态清单更新至 09-19（+静态 IP、+micro-lab 行）；待办更新（微服务 ⑧⑨ 完成、K8s YAML 深化部分完成、新增 preStop 与 node2 IP 复核）。
- 遗留尾巴：node2 IP 收敛复核、快照（双节点静态IP+探针完成）、preStop（可选）。
- 09-19 晚追加答疑（用户复习提问）："探针与零中断的关系 / actuator 作用 / 三种探针是 actuator 实现的吗" → 澄清三层分工（K8s 探针机制=kubelet 执行；应用侧 Spring Boot 状态机；actuator 只暴露 HTTP 端点；tcpSocket/exec 方式不需 actuator；startupProbe 无 actuator 专用端点）——已补入《微服务实战复盘.md》9.9 答疑区 Q4。
- 09-19 深夜追加：**A 套餐（零中断完全体）进行中**——v4 YAML 已交付（三服务 + startupProbe + preStop sleep 5 + SERVER_SHUTDOWN=graceful + terminationGracePeriodSeconds:30；liveness 解放为敏感模式），待用户 apply 并做三个验证（Terminating 停留 ~5s / 优雅停机日志 / startup 探测记录）。用户期间问"K8s 有哪些状态/Terminating" → 讲解两层概念（5 Phase vs kubectl 摘要显示状态）+ Terminating 详解——已补入《虚拟机DockerK8s实战复盘.md》第八章（新增"Pod 状态速查"小节）。
- **A 套餐 ✅ 验证通过**（2026-09-19 深夜，用户确认"验证没问题"）：preStop 停留 / 优雅停机日志 / startup 记录三证据齐全。文档已新增 9.10 小节（出口侧时序 + v4 配置 + 三验证 + 完整拼图表）。零中断发布 = 入口（9.8/9.9）+ 出口（9.10）+ 启动期（9.10）全保护。
- **下一步**：D 尾巴（node2 IP 复核 + 快照）→ **B 监控之门**（已给第一步：`kubectl set env ... MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE="health,metrics"` 解锁 /actuator/metrics，看到指标清单后再决定是否上 Prometheus 展望）。注意：set env 会与 YAML 文件漂移，若长期保留需写回文件（v5）。

## B 监控之门：上半场完成，下半场挂起（09-19 深夜）

- **已完成（上半场）**：三服务解锁 `/actuator/metrics`（set env）；用户实测查看三指标——`jvm.memory.used`（≈154MB，area/id 维度）、`jvm.gc.pause`（1 次/28ms）、`http.server.requests`（COUNT 111、平均 8.7ms、uri/status 维度）；讲解了 **counter/gauge/timer 三类统计量** + **tag 下钻** + "单帧 → 时间序列"的监控本质。
- **用户决定**：Prometheus + Grafana（下半场，轻/重两方案）"先放着，后面再弄"——已挂入《微服务实战复盘.md》待办清单（含警告：metrics 暴露是 `kubectl set env` 临时加的、与 micro-lab.yaml 漂移，固化时写回 YAML v5）。
- 当日学习收束：第 8 站部署 ✅ → 探针三篇（9.8 tcpSocket / 9.9 actuator+内部状态 / 9.10 完全体）✅ → IP 静态化 E.10（node2 待复核）→ 监控之门上半场 ✅。用户劲头足（"我不要收工，我还能学"），后续按用户节奏走。

## Sentinel Dashboard 主题 ✅（09-19 深夜完成，接"选项 4"）

- **全过程**：daocloud 白名单 403（bladex 镜像被拒）→ 自建镜像路线 → **maven 直链 404 假 jar 坑**（689 字节 HTML 被当 jar，容器报 Invalid or corrupt jarfile）→ 改用 **GitHub Release**（HEAD 探测 302→200 先验证）→ 三重校验（大小/PK 魔数/zipfile -t）→ **node2 自建** Dockerfile（temurin 8-jre-alpine）→ **node1 镜像漏更新坑**（新 Pod 恰落 node1 → 继续崩；digest 对比定位）→ 补导入 → UI 打开 → order-service 接入（set env dashboard 地址）→ 图形化配流控 QPS=2 → **压测实测：200×2 + 429×18** → 实时监控曲线验证（通过 2.0/拒绝 18.0；下游 Feign 资源仅通过 2——链路层次保护可视化）。
- 文档：《微服务实战复盘.md》新增 **9.11**（部署三坑 + 接入 + 控制台导览 + 流控实战 + 限流vs熔断表）；两个文档的 Sentinel 待办均已勾选。
- 记账追加：`SPRING_CLOUD_SENTINEL_TRANSPORT_DASHBOARD` 和 metrics 暴露都是 set env 临时配置——**共 2 笔漂移待固化**（写回 micro-lab.yaml v5）。
- 下一步候选：① 彩蛋实验（Feign 资源配流控 → fallback 降级联动）② 删演示规则/收尾 ③ 快照。

## 彩蛋实验（限流×降级联动）✅ 完成（接上条候选①）

- 用户实测数据：Feign 资源（`GET:http://user-service/users/{userId}`）配 QPS=1 → 串行 12 次（~3 req/s）= **张三×4 + 兜底×8**（"1 真 2 假"规律交替，= 4 个统计秒 × 每秒 1 额度）；30 并发 = 几乎全兜底（零星张三）；**状态码全部 200**（对比 9.11 入口限流的 429）。
- 机制：BlockException → Feign 降级捕获 → UserServiceClientFallback（"用户服务暂不可用"/"降级兜底"）；`feign.sentinel.enabled=true` 是关键开关。
- 实验后待办：删 Feign 规则（入口旧规则此前已清）。
- 文档同步：**修正发现——9.11 此前仅有待办引用、正文缺失；本轮正式补写 9.11**（Dashboard 部署三坑 + 接入 + 入口流控实战 + 限流vs熔断表）+ **新增 9.12**（彩蛋实验全录：数据/机制链/入口vs出口对照）+ 端口表（dashboard 30058 ✅）+ 踩坑表 #20（假 jar 三重校验）/#21（镜像漏更新第二次）+ 待办清单勾选。
- 备用素材：Feign 接口源码确认（UserServiceClient `/users/{userId}`、Fallback 文案"用户服务暂不可用"）——本轮我读代码核实。

## Grafana 大盘全绿 ✅（09-19 深夜，监控篇收官）

- **4701 大盘通电**：Quick Facts 全亮（Uptime 24.4min / Start time **2026-09-19 17:05:21** / Heap used 13.83% / Non-Heap 7.03%），I/O Overview（Rate 0.467ops/s、Errors 0、Duration 4.57/17.9ms）、JVM Heap/Non-Heap/Total 全部有数据。仍 No data：**Utilisation、JVM Process Memory**（疑为"大盘查询的指标名与 micrometer 版本出入"= 第三方大盘适配税，可后查可不查）。
- **"为什么之前查不到"结论（回答用户"数据有延迟吗"）**：**非数据延迟**（Prometheus 15s 拉取、近实时）。两因叠加：① **gateway 的"数据空档"**——process_uptime 曲线分两段（08:53-08:55 小段 + 09:05 起新段，中间 08:55-09:05 无 Pod 运行——网关一路卡镜像/被删/重建的真空期）；② **Grafana 前端状态滞后**（变量 None→gateway 与面板查询的缓存，需 Ctrl+F5+重选变量复位）。关键证据：Start time=17:05:21 = 当前 Pod 诞生于滚动收口时刻。
- Prometheus 存量分析：`process_uptime_seconds` 图例 6 条 series = 新旧模板"考古地层"（带 application 的 08:40 起 / 不带标签的 09:05 起 / 1 条 `application="gateway-svc:9090"` 残留）；用户超预期完成"逐层排障"（大盘→面板→查询→原始请求→数据层）。
- **待办**：① 收尾快照（建议名 `监控全链路-0919`）；② 《微服务实战复盘.md》待补 **9.13 监控篇**（Prometheus+Grafana 全链路：Step5 拉镜像 daocloud 全名+retag、Step6 部署、Step7 Grafana 三步、本轮的"空档+前端状态"答疑、磁盘风暴/扩容记录更新）；③ node1 磁盘扩容悬案已解决（E.10 相关待同步：09-13 扩容卡在 growpart/pvresize 与 lvextend 之间，本案补齐四连 38G）；④ 残留：micro-lab.yaml v5 固化（set env 漂移账：dashboard 地址、metrics 暴露、common tag）。
- **✅ 文档补充完成（09-19 深夜，用户要求"把今天学习没补充的补充到文档"）**：
  - 《微服务实战复盘.md》：端口地图 +prometheus(30091)/grafana(30092)；环境拓扑图 +治理监控组件注记；学习路线 +⑩；**新增 9.13 监控篇**（链路图 / 服务端三件事 / 两部署 / 排障四坑 / 同夜三事故 F）；踩坑表 #22~#25；待办更新（监控体系✅ + v5 固化 + registries.yaml 修复）。
  - 《虚拟机DockerK8s实战复盘.md》：TOC P1-P21；**09-13 扩容悬案修正**（原记录"LVM 四连完成"实际未完成——975 行加复盘注解）；踩坑表 +P21（DiskPressure 驱逐风暴）；微服务进度 +⑩；待办区 +2（node1 扩容补完 ✅ / 快照存档）。

## Git 上传前清理（09-19，用户准备 push 到 git）

- **发现**：micro-lab 的 3 个镜像 tar（~322MB）+ 3 个 target 目录 + sentinel-lab 的 jar/tar **此前已被 `git add` 进暂存区**——.gitignore 只挡了 `backend/target/`，没覆盖 micro-lab 与 sentinel-lab，照原样提交会推 ~400MB 大文件。
- **处理**：① `git rm -r --cached -f` 把 tar、target、sentinel 制品移出索引；② 删除磁盘：micro-lab 3 个 tar + 残缺 `gateway-1.0.0.ta` + 3 个 `target/`；sentinel-lab 的 `.tar` 删、38MB `.jar` **保留**（构建素材，已忽略不入库）；③ micro-lab 根只剩 pom.xml，sentinel-lab 剩 Dockerfile + jar。
- **.gitignore 加固（已改，待用户 `git add .gitignore`）**：新增 `target/`（通用规则取代 `backend/target/`）、`micro-lab/*.tar`、`sentinel-lab/*.jar`、`sentinel-lab/*.tar`。
- 最终暂存区 = 干净源码/配置/文档/memory；教训：多模块项目 .gitignore 要覆盖**所有 module** 的 target；镜像导出物（docker save）用完即删或放入被忽略目录。

## 待学全景盘点（09-19 晚，用户提问）

- 四组：**A 主线**（迁移真实项目 ai-agent-explorer——最大一件）；**B 微服务深化**（Prometheus+Grafana 监控体系 / 配置中心深化 Namespace+回滚 / Nacos StatefulSet+PVC / Nacos 鉴权 / CI/CD 自动化）；**C K8s 基础**（ConfigMap-Secret / requests-limits / 前端打镜像 / 可选微服务 Ingress）；**D 环境小尾巴**（快照 / node2 IP 收敛复核 / v5 固化 2 笔 set env 漂移 / Docker Desktop 加速 / 免密登录 / tmux）。
- 建议顺序：D 尾巴（20 分钟清账）→ 监控体系下半场 → 真实项目迁移。
- 已同步：《虚拟机DockerK8s实战复盘.md》微服务进度描述更新（补 09-19 完整成果 + 剩余四项）。

## Prometheus + Grafana 学习启动（09-19 晚，接待学清单 B 组·监控体系下半场）

- 用户要求"讲解 + 开始学习"。已完成概念讲解：Prometheus=采集器+时序数据库+PromQL（Pull 模型、K8s 服务发现、指标名+标签+时间戳数据模型）；Grafana=可视化平台（数据源→大盘模板）；链路：Spring Boot `/actuator/prometheus` → Prometheus 抓取 → Grafana 展示。
- **已落地第一步**：三个 pom 加 `io.micrometer:micrometer-registry-prometheus`（版本由 Boot BOM 管理；代码改动完成，待重建镜像生效）。
- 学习路线 4 步：①服务端改造（重建三镜像走完整链路）②部署 Prometheus（轻量手写版：ConfigMap+Deployment+Service）③部署 Grafana（连数据源+导 JVM 大盘）④进阶（告警/node-exporter/kube-state-metrics）。
- 待办细节：重建后需更新 set env 暴露 `prometheus` 端点（`MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE`）——漂移账 +1，v5 固化时一并写回。

## 开工执行 → 构建事故 → 镜像分发卡点（09-19 深夜，进行中）

- 开工清单已交付（Step0~8）：**端口规划 Prometheus UI=30091 / Grafana UI=30092**；文件 node2 `~/monitor-lab/`；镜像 `prom/prometheus:v2.54.1`、`grafana/grafana:10.4.5`。
- **gateway 构建事故（"Unable to find main class"）取证结论**：`target/classes` 产物正常（class 720B + yml + createdFiles.lst 齐全），但 jar:jar 打出的 `.original` 只有**空目录骨架 + MANIFEST + maven 元数据（无 class）** → repackage 找不到主类 → 留 22B 空壳 jar。判定"构建瞬间产物不可见"的偶发干扰（非配置问题），修复 = 单模块重跑；**user/order 镜像不用重做**。
- **当前卡点（待用户贴证据）**：set env 触发滚动后三个新 Pod 全 **ImagePullBackOff/ErrImagePull**（老 Pod 因滚动策略未退役，**服务未断**）；`exec deploy/user-service` 测 /actuator/prometheus 得 **404**（exec 随机落老 Pod，旧镜像无该端点，非端点问题）。
- 根因候选：① import 缺口（漏做/单台/漏 `-n k8s.io` 导到 default 命名空间）② 期望镜像名不匹配 ③ node1 状态（旧悬案）。已向用户要 4 组证据：Pod 落点+期望 image / describe events / 两台 `ctr -n k8s.io images ls`（+default 交叉查）/ 操作 history。修复预案：两台补 import → 删卡住的 3 个新 Pod → 等重建。
- **✅ 修复完成（09-19 深夜）**：证据定案"错位案"——所有 Pod 都在 **node1(lccserver)**，而镜像只导入了 **node2**；node1 `/tmp` 的 tar 其实已在（07:44/07:45 **UTC** = 北京 15:44，本次 scp 的）。缺口 = **node1 未做 ctr import**。用户在 node1 补三连 import（digest 对齐 f9067053 / a143382c / 9a9e27a0）→ node2 删 3 个卡住 Pod → 重建 → **三服务新 Pod 全部 Running** ✓。
- **固化认知**（重要）：① **镜像 store 是"节点级"非集群级**——"scp + import"每台各两步，漏任一步×任一台 → Pod 调度到那台即 ImagePullBackOff（今日第 2 次罚单）；根治方向 = 私有 registry（Harbor）。② **Linux 显示 UTC**，与 Windows（北京）差 8 小时——看时间戳要换算；已建议 `timedatectl set-timezone Asia/Shanghai`。③ 老 Pod 兜底 = 滚动更新保护（新 Pod 不 Ready，老不退役）。
- 进行中：Step 5 拉取 `prom/prometheus:v2.54.1`、`grafana/grafana:10.4.5`（★两台都拉）；待验证 `/actuator/prometheus` 端点（user/order/gateway）+ 全链路 curl 回归。
- **Step5 结果（09-19 深夜）**：`ctr pull docker.io/...` 直连被拒（DNS 污染，解析到 157.240.8.36）→ 改用 **daocloud 全名拉取 + `ctr images tag` retag 成标准名** → node1 已就位（标准名与加速源名同 digest：prometheus `f6639335` / grafana `c2f484c6`）；**node2 待用户确认**。
- **registries.yaml 诊断**：内容本身正常（K3s 语法用单数 `endpoint` ✓，mirror=docker.m.daocloud.io）但**未生效**——改配置后必须**重启 k3s** 才会被 containerd 采纳（列入待办：以后专门修，修好后所有 pull 自动走加速、不再需要点名全名）。
- 下一步：**Step 6 部署 Prometheus**（YAML 已重发：ConfigMap+Deployment+Service，NodePort 30091；注意 Pod 可能调度到任意节点——两台镜像就位是前提）。

## 环境小插曲：node1 安装 codebuddy CLI（09-19 深夜）

- 用户在 node1（lccserver）执行 `curl -fsSL https://www.codebuddy.cn/cli/install.sh | bash` 装了 CodeBuddy Code 2.155.0 → 可执行文件落在 `~/.local/bin/codebuddy`；但当前 shell 里 `codebuddy: command not found`。
- 定位：**安装成功，只是 PATH 未生效**——脚本把 `export PATH="$HOME/.local/bin:$PATH"` 写进 `~/.bashrc`（改的是文件），而当前 shell 进程的 PATH 是启动时的旧快照；`source ~/.bashrc` 即生效，**无需重启机器**（重开终端/重连 SSH 也会自动带着）。
- 事实记录：node1 上从此可用 `codebuddy` CLI（/home/lcc/.local/bin/codebuddy）。
