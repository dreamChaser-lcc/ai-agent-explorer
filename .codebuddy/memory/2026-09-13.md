# 2026-09-13

## 用户环境：虚拟机与迁移咨询
- 用户在本地 VMware 虚拟机（网卡 ens33，MAC 前缀 00:0c:29；原 IP 192.168.157.128，DHCP 动态）中运行 Docker + K8s（可见 flannel.1 / cni0，说明装了 CNI 集群）。
- 咨询虚拟机跨电脑迁移方案。给出结论：整个虚拟机目录可拷贝迁移，重点风险是 IP/MAC 变化导致 K8s 证书与节点地址失效；建议新机保持 192.168.157.0/24 网段或直接配静态 IP 192.168.157.128。
- 用户决定暂时不迁移，保持现有虚拟机环境。

## 双节点 K3s 续程（09-13）
- 用户已完成：node1 快照处理 + 磁盘扩容。路径：VMware 有快照时禁止扩盘（报"无法扩展具有快照的虚拟机中的磁盘"）→ 删除快照（=合并，数据不回滚，只是失去回滚点）→ 扩盘 40G → LVM 四连（growpart/pvresize/lvextend/resize2fs）→ 补拍快照。
- 教学插曲（已带过的坑）：用户在 VMware 控制台文本终端想翻 `ip addr` 历史输出，滚轮/Shift+↑ 均无效。正解：Shift+PgUp/PgDn 是文本控制台唯一翻历史键位；无 PgUp 实体键时推荐 SSH 操作（Windows 终端滚轮可滚）+ `命令 | less`（空格/b 翻页）。VMware Tools（open-vm-tools）不解决此问题。操作规范：日常走 SSH，VMware 窗口仅救急（类比带外管理口）。
- 双节点 join 进行中：用户在 node1 敲 `kubectl get nodes` 报 `command not found` —— 属正常（kubectl 客户端只在 server 节点 node2；agent 节点不装 kubectl）。诊断 agent 是否安装：`systemctl status k3s-agent`。
- join 流程：node2 取 `/var/lib/rancher/k3s/server/node-token` + `ip -4 addr show ens33` → node1 配 `/etc/rancher/k3s/registries.yaml`（daocloud 加速，防 pause 镜像被墙）→ 一条 `curl -sfL rancher-mirror... | INSTALL_K3S_MIRROR=cn K3S_URL=... K3S_TOKEN=... sh -`（整行粘贴）→ node2 `kubectl get nodes` 验证。
- 后续规划：任务2 跨节点调度实验（镜像送 node1：方案A=node2 ctr export+scp+node1 import；方案B=私有 registry）；任务3 Ingress（K3s 自带 Traefik）；收尾拍快照+更新复盘文档。
- 2026-09-13 确认：node1 上 `systemctl status k3s-agent --no-pager` 输出 "Unit k3s-agent.service could not be found."（= systemd 中无此 unit，agent 未安装，属正常，尚未执行 join）。用户要求用子代理做了解释。下一步：node1 执行 join 四步（node2 取 token → node1 连通性+registries.yaml → curl|sh 安装 → node2 kubectl get nodes 验证）。

## 2026-09-13 收官记录（双节点集群建成）
- ✅ join 成功：node1（lccserver, 192.168.157.128）加入 node2（lccserver-node2, 192.168.157.129）的 K3s v1.36.4+k3s1。因 rancher-mirror.rancher.cn 被阿里云 CDN 下线（公共 DNS 223.5.5.5 也返回 offline.specialcdnstatus.com → 169.254.254.254），改走**离线安装**：jsdelivr 下载 install.sh（1218 行）+ scp node2 的 /usr/local/bin/k3s（75MB 同版本）+ `INSTALL_K3S_SKIP_DOWNLOAD=true` 执行 join。
- ✅ 跨节点调度：research-agent:1.1 镜像经 node2 `ctr export --platform linux/amd64` → scp → node1 `ctr import`（-n k8s.io）送达；scale 4 副本后 2 个新 Pod 成功调度到 node1 并 Running。Pod IP 段：node1=10.42.1.x / node2=10.42.0.x（flannel 每节点 /24）；NodePort 30081 在每台节点均监听。
- ✅ Pod 反亲和：缩容后两 Pod 都在 node2（默认调度不保证分散）→ 在 ~/k8s-lab/research-agent.yaml 加 podAntiAffinity（preferred 软约束）实现"一节点一个"。
- 新坑（已写进复盘文档 P16-P18）：① ctr export 对 OCI index 镜像必须加 --platform，否则产出截断 tar（Unexpected EOF / import 报 unrecognized image format）；② tar 完整性验证法：`tar -tf f.tar > /dev/null && echo 完整`；③ sudo 创建的 tar 需 sudo rm；④ node1 上 kubectl 报 localhost:8080 refused = agent 有 kubectl 符号链接但无 kubeconfig（正常，钥匙只在 server）；⑤ 宿主机 VPN 会影响 NAT 虚拟机 DNS（关闭后需清缓存）。
- 文档更新（09-13）：复盘文档（docs/服务架构/虚拟机DockerK8s实战复盘.md）新增 E.6/E.7/E.8（双节点 join 含离线安装/跨节点调度/反亲和）、踩坑 P16-P18、概念速查新增条目；修正 E.1（补 export KUBECONFIG，P12/P15 同步）；更新架构图/1.3 IP（node1=.128，node2=.129）/九章状态清单/十章待办（已完成项勾选，新增可选待办）。
- 下一步：任务3 Ingress（K3s 自带 Traefik，域名 research.local → research-agent-svc:8080，用 curl -H "Host: ..." 测试）。

## 微服务学习启动（2026-09-13，用户选定 B 方案）
- 用户决定开始学微服务，选择 **B 方案**：新建迷你教学项目练机制（而非直接拆真实项目）。
- 已创建 **micro-lab 教学工程**（`d:\project\lcc-github\ai-agent-explorer\micro-lab\`），Maven 多模块：
  - 父 pom：三件套 BOM 统一版本 —— **Boot 3.2.4 + Cloud 2023.0.1 + Alibaba 2023.0.1.0**（官方适配组合，注释里标了"三件套必须配套"的坑）；
  - user-service（:8081，GET /users/{userId}，内存 Map 数据）；
  - order-service（:8082，GET /orders/{orderId}，OrderInfo 含 userId 为第 4 站 Feign 调用埋伏笔）；
  - 均为纯 Boot（无 Nacos 依赖），pom 注释标注了后续站点加依赖的位置；代码风格：中文注释、多词大驼峰、record 模型。
- 微服务路线（8 站）：① Nacos 上 K8s（nacos.yaml 已给，Deployment+NodePort 30048/30049，待用户执行）→ ② 工程骨架（已完成）→ ③ 注册发现 → ④ OpenFeign → ⑤ 配置中心 → ⑥ 网关（Gateway，与 Ingress 分工）→ ⑦ Sentinel → ⑧ 全量上 K8s。
- 教学项目位置说明：放在 ai-agent-explorer 仓库内（micro-lab/ 子目录），方便 IDE 协作开发；用户如不想混入主仓库可后续移出/加 .gitignore。

## 第 3 站：服务注册（Nacos Discovery）启动
- Nacos 已在 K8s 部署（用户之前 apply 过；再次 apply 输出 unchanged = 声明式幂等）。⚠️ **重要修正**：Nacos 2.x 客户端 gRPC 端口 = 配置端口 + 1000，因此 Service 的 grpc nodePort 必须是 **31048（=30048+1000）**，原来给的 30049 会导致 Windows 本地服务注册时 gRPC 连不上。已让用户 `sed -i 's/nodePort: 30049/nodePort: 31048/' ~/k8s-lab/nacos.yaml` 后重新 apply。
- micro-lab 已加 Nacos 依赖与配置（4 文件改动）：两个 pom 加 `spring-cloud-starter-alibaba-nacos-discovery`（版本由父 pom Alibaba BOM 管）；两个 application.yml 加 `spring.cloud.nacos.discovery.server-addr: 192.168.157.129:30048`。无需 @EnableDiscoveryClient（2020+ 自动装配）。
- 验证方式：启动日志搜 `register finished`；Nacos 控制台（http://192.168.157.129:30048/nacos）服务列表看 user-service / order-service。
- 下一步：第 4 站 OpenFeign（order 调 user）。

## 第 4 站：OpenFeign 服务间调用（代码已就位）
- micro-lab order-service 改动（5 处）：pom 加 `spring-cloud-starter-openfeign` + `spring-cloud-starter-loadbalancer`；启动类加 `@EnableFeignClients`；新增 `client/UserServiceClient`（`@FeignClient(name="user-service")`）；新增 DTO `UserProfileView` + `OrderDetailView`；`OrderController` 注入 Feign 客户端并把订单与用户信息聚合返回（/orders/{orderId} 返回 OrderDetailView）。
- 验证方式：user-service 保持运行，重启 order-service，`curl http://localhost:8082/orders/1001` 期望返回含 userProfile 的聚合 JSON。
- 埋的伏笔实验：停掉 user-service 后调 order 会 500（服务依赖雪崩风险），引出第 7 站 Sentinel。
- 注意：micro-lab 目前未被 IDE 识别为 Maven 项目（lint 报 non-project file / package 不匹配系误报），需要时在 IDE 中导入为 Maven 项目。
- 备注：micro-lab 第 3 站已通关（两个服务注册进 Nacos，日志 register finished，控制台服务列表可见）。

## 第 5/6 站进度（2026-09-13 晚）
- 第 5 站（Nacos 配置中心）**已通关**：user-service 加 nacos-config 依赖 + `spring.config.import: optional:nacos:user-service.yaml`；新增 UserConfigController（@RefreshScope + @Value）；用户在 Nacos 控制台建了 user-service.yaml 配置；**动态刷新实测成功**（改配置后 curl 立即返回新值，服务未重启）。
- 第 6 站（Spring Cloud Gateway）：新增 gateway 模块（父 pom modules 已加）。依赖：spring-cloud-starter-gateway + nacos-discovery + loadbalancer（⚠️ 不能引 spring-boot-starter-web）。路由：/api/users/** → lb://user-service、/api/orders/** → lb://order-service，均 StripPrefix=1。
- ⚠️ 端口：8080 在用户 Windows 上已被占用（疑似 backend 或其他进程），**gateway 改用 9090**。端口地图：gateway=9090、user=8081、order=8082。
- 待验证：gateway 启动 + curl 9090 三条（users/orders/404 反例）。
- 遗留：第 7 站 Sentinel 熔断限流（伏笔：停 user 后 order 报 500）。

## 第 7 站完成 + 第 8 站启动（2026-09-13 深夜）
- 第 7 站 Sentinel 代码就位（用户未贴验证，默认 OK）：order-service 加 `spring-cloud-starter-alibaba-sentinel`；yml 加 `spring.cloud.sentinel.eager/transport` + **`feign.sentinel.enabled=true`（关键开关）**；`UserServiceClient` 加 `fallback = UserServiceClientFallback.class`；新增 UserServiceClientFallback（返回"用户服务暂不可用/降级兜底"）。
- 📌 用户登记待办：**Sentinel Dashboard 部署**（Docker: docker.m.daocloud.io/bladex/sentinel-dashboard:1.8.8 → 8858；已写入复盘文档待办区）。
- 第 8 站启动（收官）：把 micro-lab 三服务部署到 K3s。设计：本地 mvn package → 各服务单阶段 Dockerfile（FROM daocloud 的 temurin:21-jre-alpine，已创建）→ 构建 3 镜像（user-service:1.0.0 / order-service:1.0.0 / gateway:1.0.0）→ save/scp/ctr import → K8s YAML（3 Deployment + 3 Service：**gateway NodePort 30090 对外，user/order ClusterIP 内部**）→ K8s 内 Nacos 地址用 **nacos-svc:8848**（用环境变量覆盖：SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR / SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR，12-Factor 配置外置）→ **部署前须停掉 Windows 本地三个服务**（避免同服务名本地/K8s 实例混注册）。
- 三个镜像已在 Windows 构建完成（user-service:1.0.0 / order-service:1.0.0 / gateway:1.0.0）；**第 8 站（镜像搬运 + 部署 YAML）用户计划明天实践**。

## 文档产出（2026-09-13 深夜）
- 新建 `docs/服务架构/微服务实战复盘.md`：micro-lab 第 1-7 站完整实战记录（原理/命令/代码/验证/踩坑）+ 第 8 站全量待实践步骤（构建镜像/导出传输/双节点导入/部署 YAML/验证/排障表）+ 命令速查 + 待办清单（含 Sentinel Dashboard）。
- 复盘文档《虚拟机DockerK8s实战复盘.md》的"微服务改造"待办条目已加交叉引用指向新文档。
