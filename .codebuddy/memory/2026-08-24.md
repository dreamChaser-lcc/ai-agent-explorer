# 2026-08-24

## SQL 学习文档
- 用户在系统学习 SQL（CRUD、JOIN、视图、外键、Flyway、PG/MySQL 差异），基于本项目的 8 张表进行讲解教学。
- 已生成学习手册：`docs/SQL学习手册-CRUD与多表查询.md`，供用户后续回顾。
- 项目数据库现状：dev 用 H2（PostgreSQL 兼容模式），prod 用 PostgreSQL，Flyway 管理迁移；Redis 依赖已引入但代码零使用。

## Redis 缓存落地（2026-08-26）
- 已落地 Redis 缓存示例：新建 `infrastructure/config/RedisCacheConfig.java`（@EnableCaching + RedisCacheManager，JSON 序列化，默认 30 秒 TTL）。
- 给 `ResearchTaskQueryService.getTaskDetail` 加 `@Cacheable(value="task:detail", key="#taskId")`。
- 注意：配置类名避开 Spring 自带的 `RedisCacheConfiguration`（同名冲突），故命名 `RedisCacheConfig`。
- 已生成文档：`docs/Redis使用策略与实战手册.md`。
- dev 模式（H2）本地未启动 Redis 时缓存会连接失败，需注意。
