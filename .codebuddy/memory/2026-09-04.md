# 2026-09-04

## 后端概念系统性教学会话
- 用户进行了长时间、覆盖 Java/Spring 后端的系统性问答学习（数据库、SQL、Redis、Bean/注解、Spring MVC、REST/RPC、DTO、Tomcat/Servlet、线程模型、高并发）。
- 用户反馈速记版太简略，已把 `docs/Java后端问答知识汇总.md` 重写为**详细讲义版**（约 800 行）：每个主题按"是什么→为什么→代码示例→流程图→易错点"展开，含数据库认知、SQL、Redis、Java/Spring 基础、Spring MVC/Tomcat/线程模型/高并发 5 大章 + 口诀速记。
- 之前 8 月已产出：`docs/SQL学习手册-CRUD与多表查询.md`、`docs/Redis使用策略与实战手册.md`。
- Windows 已装 Redis（`D:\softwares\Redis`，服务 Running，PATH 已配置到用户级）。
- ⚠️ 纠正过时记忆：Redis 缓存代码（RedisCacheConfig.java、pom.xml redis 依赖、ResearchTaskQueryService 的 @Cacheable）已在本会话之前被提交 `d163142 chore: 优化本地开发配置，完善文档，移除Redis缓存` **整体移除**，当前代码库无 Redis 缓存实现。仅 `application.yml` 仍残留 spring.data.redis 连接配置（host/port/password），属预留配置。docs 讲义第三部分标注了该历史。
- 决策（用户确认）：保持现状，**不恢复 Redis 缓存**。代码库维持无 Redis 缓存实现状态，docs 文档保留讲解并已加"未启用"警示。
- 应用户要求把 `docs/Java后端问答知识汇总.md` 的 5.13 高并发章节扩写为 10 小节专题讲义（指标定义、Little's Law 演算、瓶颈排队、四板斧详解含限流算法/MQ/熔断、数据库侧、压测、演进路线、本项目盘点、误区澄清）。
