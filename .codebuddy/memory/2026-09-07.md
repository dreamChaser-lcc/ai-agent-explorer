# 2026-09-07（含 09-08 续）

## 虚拟机与容器学习计划（已全部通关 2026-09-08）
- 用户已读 `docs/服务架构/虚拟机与企业级架构学习笔记.md`，确认 ai-agent-explorer 项目已可跑通，开始进入"虚拟机 + 容器"学习阶段。
- 已制定学习计划：先容器后虚拟机（Docker Desktop 底层靠 WSL2，顺带入门虚拟化）。
  - 第 1 步：WSL2（`wsl --install`）
  - 第 2 步：Docker Desktop → 容器操作 → Volume 挂载实验 → 给 backend 写多阶段 Dockerfile
  - 第 3 步：VMware Workstation Pro（装 D:\VMware，磁盘放 D:\VirtualMachines）+ Ubuntu 22.04 Server 虚拟机 + SSH + 虚拟机内装 Docker，再克隆多节点模拟集群
- 待办：用户装完 Docker Desktop 后可帮其在 `backend/` 下创建多阶段 Dockerfile（maven 编译 + temurin JRE alpine 运行）。
- 用户反馈偏好（延续 9-04/9-05）：文档要详细展开，不要要点罗列。

## 虚拟机安装进度（当日完成）
- VMware Workstation Pro 已装在 `D:\softwares\VMware`；虚拟机文件在 `D:\softwares\Virtual Machines\Ubuntu 64 位\`。
- 走了"简易安装"模式（ISO 直装），Ubuntu 22.04.5 Server 安装成功；用户名 `lcc`，主机名 `lccserver`。
- NAT 模式，虚拟机当前 IP 为 192.168.157.128（用户明确指定以此为准，不要再用 .157）；IP 仍可能重启后变化，需提醒用户注意。SSH 连接验证成功。
- 讲过的知识点：ISO 是什么、快照/克隆价值、NAT 私有 IP vs 公网 IP、SSH 连虚拟机=连真实服务器同款姿势、VMware 需常开但操作走 SSH。
- ~~下一步待办~~ 已完成：docker.io + docker-compose-v2 装好，Docker Hub 拉取被墙（DNS 污染），已配 daemon.json 镜像加速（daocloud/dockerproxy），`docker run hello-world` 验证成功。ufw 保持未启用（用户决定）。快照"纯净系统-SSH可用"已拍，"Ubuntu-Docker就绪"快照待用户补拍。
- 讲过的知识点追加：sudo 与免 sudo（usermod -aG docker）、Xshell vs PowerShell SSH（本质同为 SSH 客户端，推荐进阶用 VS Code Remote-SSH）。
- 下一阶段三选项已给出，用户选定顺序 A → C → B。
- A 阶段（容器实战）已完成：Nginx 容器+端口映射、exec 进容器、MySQL 8.0 容器+Volume 挂载（/home/lcc/docker-data/mysql）、删容器数据复活实验成功。期间踩坑：多行命令粘贴丢失 -v 导致数据"丢失"，已排查并复盘（生产事故级教训）。
- C 阶段（Nacos）已完成：v2.3.2 容器部署（8848/9848 端口、standalone、Volume 挂载），发现 Nacos 2.2+ 默认不鉴权（无需登录）的知识点，用户选择保持默认；已在控制台发布 demo-config.yaml。A、C 均通关。
- B 阶段进行中：已完整克隆 node2（完整克隆，路径 D:\softwares\Virtual Machines\ubuntu-node2），node2 主机名已改 lccserver-node2。踩坑复盘：克隆后 docker ps 为空 → 容器是停止状态（docker ps 只显示运行中），需 docker start；已教 --restart=always 企业级自启策略（用户在执行中）。
- 讲过的知识点追加：Nacos 2.x 默认不鉴权原因与企业鉴权配置（NACOS_AUTH_ENABLE 等）、-v 宿主机目录不存在时自动创建（root 属主坑）、目录挂载空目录会遮盖容器内默认配置、跳板机本质（内网无公网 IP+管控审计，ssh -J 代理跳转）、完整克隆 vs 链接克隆。
- 明日待办：① 确认 node2 容器启动+--restart=always 配置 ② node1↔node2 互联验证（ping/ssh 跳转/跨节点 curl Nacos）③ 可加练 ssh-keygen + ssh-copy-id 密钥登录、ssh -J ④ 全部完成后拍快照，虚拟机阶段通关，之后回 Docker Desktop 路线或开始 K8s（Docker Desktop 自带 K8s 或 Minikube）。
- 用户明确约定（重要偏好）：教学中的"偷懒步骤"（如关防火墙、免密、root 直登等不符合企业级常规的操作）必须提前说明并给出企业级 vs 简化版选项让用户选择，不能默认走简化路径。用户本次选择不启用 ufw（保持 inactive），学习环境仅此一次例外。
